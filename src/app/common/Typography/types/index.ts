export type TypographyProps = {
  children: any;
  className?: string;
  /** Any valid CSS color (hex, rgb, named). Overrides theme title color when set. */
  color?: string;
  onClick?: () => void;
};
